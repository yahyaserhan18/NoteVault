# Database
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/notes_app?schema=public"

# Frontend URL allowed by CORS
FRONTEND_URL="http://localhost:5173"

# JWT — Access Token (short-lived, e.g. 15m, 1h)
JWT_ACCESS_SECRET="your-access-token-secret-min-32-chars-here"
JWT_ACCESS_EXPIRY="15m"

# JWT — Refresh Token (long-lived, e.g. 7d, 30d)
JWT_REFRESH_SECRET="your-refresh-token-secret-min-32-chars-here"
JWT_REFRESH_EXPIRY="7d"